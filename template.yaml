# Redirect to infrastructure folder
# Use: infrastructure/template-full.yaml for complete deployment
# Use: infrastructure/template-core.yaml + modules for modular deployment

# Quick deployment commands:
# make deploy              - Full stack
# make deploy-modular      - Core + Security Hub
# make deploy-core         - Core only
# make deploy-security     - Security Hub only (requires core)
